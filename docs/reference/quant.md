# spyx.quant

Thin SNN-aware wrapper around Google's [qwix](https://github.com/google/qwix) quantization library. Requires the optional `qwix` dependency (`uv pip install "git+https://github.com/google/qwix"`); without it, importing is safe but the helpers raise `ImportError`.

::: spyx.quant
